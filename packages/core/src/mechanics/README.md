# Deterministic mechanics boundary

Owns repository-root discovery, filesystem inventory, content identity, optional Git observation, redaction, atomic writes, and deterministic path/link/document comparison helpers.

These helpers report observable mechanics only. They do not classify repository architecture, infer product meaning, execute target-application code, or own orchestration semantics.
