# Node API fixture

The API starts at `services/api/src/server.js`. A deliberately retained legacy
fallback lives in `services/api/src/legacy-server.js`. Shared types live at
`packages/shared/src/index.js`.
